#!/usr/bin/env bash
# focus-banner.sh — idle screen of the omo-focus viewer's welcome tab.
while :; do
  clear
  printf '\n\n   omo-pulse · focus viewer\n\n   waiting for a focus request —\n   click a session in the focus remote\n\n'
  sleep 3600
done
