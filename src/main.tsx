import './styles/index.css';
import { StrictMode } from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <div>ez-omo-dash loading...</div>
  </StrictMode>
);
